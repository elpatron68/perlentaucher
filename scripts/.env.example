# Codeberg API Token für Release-Scripts
# Kopiere diese Datei zu .env und füge dein Token ein:
# cp .env.example .env
# 
# Erstelle einen Personal Access Token unter:
# https://codeberg.org/user/settings/applications
# Benötigte Scopes: 'public_repo' oder 'repo' (für private Repos)
CODEBERG_TOKEN=dein-token-hier
